version: "3.9"
services:
  self_building_ai:
    build: .
    container_name: self_building_ai_container
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1